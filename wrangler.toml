# Cloudflare Pages — optional CLI: npx wrangler pages deploy dist --project-name=YOUR_PROJECT
# For Git-connected projects, set the same build/output in the dashboard (see README).
name = "smartcopybook"
compatibility_date = "2024-11-01"
pages_build_output_dir = "dist"
